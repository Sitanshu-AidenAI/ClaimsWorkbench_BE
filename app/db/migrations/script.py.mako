"""${message}

Revision ID: ${up_revision}
Revises: ${down_revision | comma,n}
Create Date: ${create_date}

Migrations are hand-written SQL. Use `op.execute()` with explicit statements
rather than autogenerated operations, so the exact DDL that runs in production
is reviewable in the diff.
"""

from collections.abc import Sequence

from alembic import op

revision: str = ${repr(up_revision)}
down_revision: str | None = ${repr(down_revision)}
branch_labels: str | Sequence[str] | None = ${repr(branch_labels)}
depends_on: str | Sequence[str] | None = ${repr(depends_on)}


def upgrade() -> None:
    ${upgrades if upgrades else "op.execute(\"\"\"\n    \"\"\")"}


def downgrade() -> None:
    ${downgrades if downgrades else "op.execute(\"\"\"\n    \"\"\")"}
